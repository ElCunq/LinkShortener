{
    on_demand_tls {
        ask http://admin-panel:3000/internal/domain-check
    }
}

# Catch-all HTTPS for all verified custom shortlink domains
https:// {
    tls {
        on_demand
    }

    reverse_proxy shortener-engine:3000
}

# Catch-all HTTP (redirects or proxies)
http:// {
    reverse_proxy shortener-engine:3000
}
